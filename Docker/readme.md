This folder contains scripts and notes for Docker and Kubernetes commands that 
1. Build the container image,
2. Save the image to a private registry (Azure Container Registry), 
3. Set up a Kubernetes container cluster, and
4. Deploy the image as a container in the cluster.
